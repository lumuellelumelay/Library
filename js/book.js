export default class Book {
  constructor(id, title, author, year, description, progress, url) {
    this.id = id;
    this.title = title;
    this.author = author;
    this.year = year;
    this.description = description;
    this.progress = progress;
    this.url = url;
  }
}
